[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-mlops-platform"
version = "1.0.0"
description = "Enterprise MLOps platform with experiment tracking and model management"
requires-python = ">=3.10"

[project.scripts]
app = "app:app"
